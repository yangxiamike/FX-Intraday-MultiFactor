# Notebooks

Use this directory for research notebooks that call into the library modules under `src/fx_multi_factor`.

All notebook demos and validation runs should prefer real fixture samples fetched from Massive/Polygon, not generated mock market data.

The initial scaffold includes:

- `01_usdjpy_research.py`: notebook-friendly script for bootstrapping the USDJPY v1 demo flow against a real fixture sample
